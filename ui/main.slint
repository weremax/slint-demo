export component MainWindow {
    width: 300px;
    height: 150px;

    Text {
        text: "Hello Slint";
        font-size: 30px;
        color: white;
        vertical-alignment: center;
        horizontal-alignment: center;
    }
}